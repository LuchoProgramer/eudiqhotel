import { ReactNode } from 'react';

export const metadata = {
  title: 'Servicios | Eudiq Hotel Loja',
  description: 'Conoce todos los servicios de Eudiq Hotel Loja: Wi-Fi, desayuno buffet, estacionamiento, Café Viviates, lavandería y más.',
};

export default function ServiciosLayout({
  children,
}: {
  children: ReactNode;
}) {
  return children;
}